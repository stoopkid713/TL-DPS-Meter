__pycache__/
*.pyc
.venv/
.pytest_cache/
# Generated fixtures (regenerate via tools/build_sample_fixture.py)
fixtures/sample_*.json
fixtures/_*.json
# Parity fixtures captured from the old .exe live in the private oracle, NOT here.
# Copy oracle/fixtures/gold_* into fixtures/ to run the parity suite locally; the
# tests skip when absent. Ignored so a local copy never gets committed.
fixtures/gold_*
# PyInstaller build outputs (Phase 8) — exe + work dir are artifacts, never tracked.
# (root .gitignore only anchors the repo-root /dist/, not backend/dist/.)
dist/
build/
# Diagnostics runtime artifacts + local dev scratch tools (debug.py IS tracked).
tldps-debug*.jsonl
_monitor.log
_monitor.py
_repro_reset.py
